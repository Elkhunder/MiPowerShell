using System;
using System.Collections.Generic;
using System.Linq;
using System.Security;
using System.Windows.Forms;
using System.Text;
using System.Threading.Tasks;

namespace MiPowerShell.Arguments.BiosCommands
{
    public class ClearBiosPasswordArguments
    {
        //public ClearBiosPasswordArguments(SecureString biosPassword, string[] computerNames, DialogResult remote, DialogResult local) : base(biosPassword, computerNames, remote, local)
        //{
        //}
        //public new void Deconstruct(out SecureString biosPassword, out string[] computerNames, out DialogResult remote, out DialogResult local)
        //{
        //    base.Deconstruct(out biosPassword, out computerNames, out remote, out local);
        //}
    }
}
